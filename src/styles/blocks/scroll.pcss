.scroll {
  @include centered('x');
  bottom: 20px;
  display: flex;
}

.scroll__text {
  font-size: 18px;
  font-weight: 400;
  margin-right: 20px;
}

.scroll__icon {
  fill: $text-color;
  width: 22px;
  height: 30px;
}